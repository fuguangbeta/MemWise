# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.0 (2026年8月)

> 本轮的主题是"机制修复与体验精修"——经过 61 轮对话的持续审查与迭代，从决策链到界面交互进行了全量深度审查：恢复了长期失效的策略投票与清理反馈通道，修复 EFIS 单向漂移与诊断输入缺失，重塑守护节奏为"低频大额收割"，游戏模式达成"进程零触碰 + 系统缓存零磁盘干扰"的静音化，图表数据如实化，看门狗真正可重启，并修复了窗口标题栏图标长期失效与崩溃日志误导性记录两个用户可见问题。累积修复 90+ 项，回归测试扩展至 37 项并全绿，因此定义为大版本 3.0。

---

### 决策链恢复：策略投票与学习反馈复活

策略投票引擎此前因 `score` 未初始化与调用不存在的 `kalman_advantage` 双异常，导致除首轮外所有清理决策被"投票异常"拒绝；清理反馈错走试探通道，收益/成本/清理计数全部失真。

- **`policy.should_trim` 重写**：真实计分（树1 收益 / 树2 压力 / 树5 反事实对比画像均值），各树贡献返回供在线权重学习
- **`ProcessSnapshot.__slots__` 补 `_tree_scores`**：投票结果挂载快照，权重学习链路打通
- **`record_clean_result` 改走 `record_clean`**：`gain_ewma`/`cost_ewma`/清理计数恢复更新，连带救活 ROI、θ 收敛、概念漂移检测与 ERIS 维度1
- **`can_probe` 补黑名单检查**：排除列表承诺"不参与试探性清理"从此真实生效
- **元认知漂移复位置 `_theta_dirty`**：θ 缓存不再滞后于复位
- **回归测试扩展 33 → 37 项**：新增非首轮策略投票、清理反馈通道、EFIS 负面调整、EFIS 参数表 9 键四条断言

### EFIS 闭环：双向调参与真实诊断

- **症状累积方向缺陷修复**：负面调整原本永不生效（参数单向漂移到上限），改为方向感知累积（`prev + (±1)`）
- **守护诊断输入补齐 11 字段**：θ 均值/θ>0.6 占比/agg/PF 增量/周期耗时/deepen/layer3/冷却/失败，全部现成数据零新增采集（judger 增 `pf_delta_total` 累计、cleaner 增 `deepen_extra` 统计）
- **`layer3_agg_gate` 接入 normal 门槛**：EFIS 真正控制 Layer3 触发；`interval_high` 移除（功能被 gap 自适应覆盖，参数表 10→9）；`learning_rate` 标注保留待实验；`compress` 名义缺失注明（与 `standby_low` 同为内核操作码 5，由后者覆盖）
- **Layer3 诊断改周期增量口径**：累计值曾导致 gate 持续爬升；PF 计数每周期无条件清零防跨周期污染
- **ERIS 维度4 数据源修正**：`self.efis`（原读 `learner.efis` 恒 None，维度4 永远中性）

### 守护节奏分层：低频大额收割

高频压制原每 0.5s 清空 standby——文件缓存永远失效、磁盘反复写盘、自身工作集颠簸，花销远超收益。

- **高频阶段（3s）仅注册表缓存**（零磁盘 I/O）；standby 与脏页写回由 gap 末 optimize（8-25s，有累积窗口）与 harvest（60s）统一执行
- **自身工作集清空 0.5s → 60s**（`clean_self` 参数，高频路径关闭，harvest 低频兜底）
- **快照节奏对齐 3s**（`snap_skip` 调整，采集/学习频率与决策节奏一致）
- **系统杂项统计真实化**：standby/脏页等按 API 实际成功计数（权限不足不再虚增），`compress` 虚计移除
- **Layer3 阶段 C 精简**：standby 系列 7 次内核调用合并为单次深度清空；`combine` 独立条件修正（原挂在 standby 下只勾内存合并时不执行）
- **gap-fill 路径禁用 Layer3**（`allow_layer3=False`，深度操作只在 harvest 执行）
- **周期从等待开始计**（修复每小时 3 分钟漂移）；热加载同步 `judger.cfg`（排除列表/清理深度/EFIS 参数守护期间即时生效）；gap 自适应改纯进程口径（系统级释放不再稀释）；退出时 join daemon 线程；保存链分离 try

### 游戏模式静音化：进程零触碰 + 缓存零干扰

- **PID 精确保护覆盖全路径**：trim/probe/Layer3/横扫；游戏运行期间新增实例每 3s 刷新保护集
- **系统缓存全跳过**（仅注册表）；gap-fill 与收尾循环不再清 standby/脏页——消除周期性读盘卡顿（都市天际线类读盘密集游戏实测根因）
- **每轮完整收割**（降频机制已删除）：图表每轮真实数据，非游戏进程按智能评分持续清理
- **实时启停检测**（3s 快照，日志周期末按序打包，周期内启停日志按时间顺序输出）；前台保护恢复（fg_gate 0.35）；失败冷却恢复常规；游戏模式 probe 全拒
- **输入人性化**：`.exe` 后缀自动补全、重复项提示、tooltip 引导"同名实例全保护"与辅助程序排除建议

### 手动优化与图表

- 手动优化异常兜底（按钮不再永锁）、三轮真实汇总（含系统级净释放）、遵守 `clean_operations`、守护中一键转为即时轻量清理（防重入 + 游戏跳过）、死代码删除（`inc_freed`/`inc_mb`/不可达守护分支）
- 图表 meta 扩为 6 元组（含收割标记）；未收割周期**如实进 ERIS 分布窗**（效率真实反映 0 释放）并在悬浮提示标注原因；死代码/死变量清零（14 处：`recent_perf`/`pressure`/`total_attempts`/`his_peak`/`eff_text`/`_eris_sub`/`barhit` 空对象等）；游戏切换 p50 预填充加速冷启动；悬浮因子对齐复查
- CLI 显示"本次净释放"口径 + `--mode` 校验回退；CLI 守护热加载同步 `judger.cfg`（排除/深度/EFIS 与 GUI 对齐）；CLI 清理调用补全 `operations`/`aggressiveness` 参数；`--scheduled`/`--interval-clean` 随功能移除

### 看门狗与崩溃诊断

- **看门狗真实可用**：`OpenProcess` 失败视为进程已死走重启（原永不重启）；脚本模式命令与路径修复；`_watchdog_path()` 统一
- **崩溃日志去误导**：启动头标注"程序启动"（原"=== 时间 ==="被误认为崩溃）、看门狗进程初始化前跳过（不再混写）、异常加"未捕获异常"前缀、512KB 自动轮转、新增 `_diag_log` 诊断通道
- **窗口标题栏图标失效根因修复**：Tk 双层窗口结构（`winfo_id` 是 client 窗口，标题栏图标须发 wrapper 顶层；图标在窗口层级建立前执行导致 `GetParent` 返回 0）——延迟 100ms + `GetAncestor` 取顶层 + 双设兜底，主窗口与全部弹窗修复；弹窗类图标副作用移除（防泄漏与互相覆盖）
- **退出流程**：正常退出写看门狗标记、join 线程、保存链分离

### 界面、按钮与输入

- **弹窗一次性居中**（`_center_geometry` 9 处 + 主窗口，首次出现即居中零闪烁，屏幕边界钳制，排行窗口偏置修正）
- 设置面板补"注册表缓存清理"开关（8 操作独立开关名副其实）；checkbox 初始化统一（"全关"状态可表达）；自启双向互斥（杜绝开机双启动）；管理员自启状态如实化
- 全局热键支持配置自定义 + 失败提示 + 设置面板入口 + 即时重注册（`_apply_hotkey`）
- 排除列表即时保存、窗口残留清理（原 `inner_frame` NameError 导致窗口半残）、关闭按钮补全
- UI 纯中文化：`EmptyWorkingSet`→释放进程闲置内存、`Standby List`→待机缓存清理、`Modified Page`→脏页写回、`memwise.log`、`pass`→轮、`试探OK`→试探成功、列名规范（进程号/内存占用/CPU占用）
- 进程排行交互（排序/终止确认）复查；`_on_toggle_game` 重复赋值清理；`_validate.py` 标注废弃；内存合并 tooltip 如实化（部分系统版本支持有限）
- 定时清理与间隔清理功能按实际使用评估后移除（与 full 模式重叠，回归简洁）

### 兼容与性能

- **NtQuery 进程布局运行时自校验**：实测本机真实布局（0x50/0x90/0xB8）与旧硬编码完全不同——批量内存读取从未真正生效，全靠逐进程兜底；自校验后 219 进程一次读取，性能与数据源统一
- 打包配置模板兜底（exe 独立部署首次运行即有完整配置）；路径缓存上限 200→512（消除删-建抖动）；卷句柄无效判断修复；Layer3 等待加超时（8s）；CLI `status` 画像行恢复（`json` 导入补齐）；崩溃日志路径统一 exe 目录；`get_last_input_tick` 等死函数清理
- **全项目文档一致性**：README 游戏模式节重写（横扫/无条件清空等旧描述与实现对齐）、ERIS 参数段更新（v6/scale=40/维度窗口/log10 压缩）、tooltip 与日志全中文规范复查

### 文档与发布

- **更新日志版本策略变更**（自 v3.0 起）：不保留旧版本章节，每次大版本整体重写，覆盖该版本全部变更
- **README 全量更新**：18 处描述修正（产物 12.7→13.3MB、gap-fill 仅注册表、三树真实结构、遗忘 3%→5%、泄漏仅观察、元认知每周期、排行列名/3 秒/私有内存、学习日志按名、8 操作开关补 filecache、CLI `--mode`、interval 默认 30/60、EFIS 9 参数、文件结构补 test_v2.6 等）+ 3 处补充（紧急触发、全局热键、即时生效说明）+ 2 处删除（EFIS 场景记忆）+ 排版统一（3.1 标题中英对照、紧急触发英文段）
- **GitHub v3.0 Release 发布**：README/CHANGELOG 推送 main、tag `v3.0`、release + `MemWise.exe` 资产（13.26MB）、Release Body 取 CHANGELOG 全文
- **项目记忆文件同步更新**：项目百科、构建信息、GitHub 发布工作流（上传须走 `uploads.github.com`）、发布不碰旧版本红线、更新日志规则、工作方式（自动启动授权/构建节制）

---

### 构建

- 版本号 v2.7 → v3.0（Mutex/标题/窗口名/日志/README/测试同步），PyInstaller 6.21.0，Python 3.14.0，产物约 13.3 MB
- 回归测试扩展至 37 项（新增非首轮投票/反馈通道/EFIS 双向/参数表 9 键断言）
- 37 项回归测试全绿，全项目零编译错误
