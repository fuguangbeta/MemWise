# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v4.0.128 (2026年8月)

> v3.9.28 发布后的维护增量与全新双语界面，共 128 项。对标同类产品完成两轮深度挖掘与多轮全量核查，新增完整中英双语界面（可即时切换）；清理安全多重保护门、稳态锚点与回弹学习、四模式真实力度分层全部落地；界面细节与稳定性全面修正。回归测试 134 项全绿。

### 语言支持

> 新增完整双语界面。设置面板顶部独立"语言"栏目，可即时切换简体中文与 English，全界面跟随语言变化，未覆盖内容自动回退原语言。

- 新增了界面语言选项，默认简体中文，可即时切换 English
- 新增了全界面翻译，提示、对话框、日志显示、命令行全部跟随语言
- 优化了多语言切换体验，切换瞬间生效，守护与统计数据不受影响
- 优化了日志的多语言适配，显示随语言切换，日志文件保留原始语言便于排障
- 新增了语言切换的安全保护，切换失败自动保留原界面

### 清理安全与体验

> 多重保护门覆盖清理全程。刚切走的程序、正在工作的程序、挂着窗口的后台程序不再被误清；判定与执行两层防护，多开实例互不干扰。

- 修复了刚切走的程序可能被立刻清理的问题
- 修复了正在工作的程序可能被清理的问题，CPU 或磁盘读写活跃的一律跳过
- 优化了后台窗口程序的保护，挂着窗口的后台程序保护时间更长
- 优化了清理判定，连续两轮确认空闲才纳入候选
- 优化了清理确认，进程重启后重新确认，不继承旧实例状态
- 优化了清理确认粒度，同一程序多开实例互不干扰
- 优化了执行前安全检查，清理瞬间切到前台、游戏启动或开始下载会即时取消
- 优化了深度清理防护，深度聚合同样要求空闲确认与磁盘活跃检查
- 优化了间隙快速重清的防护，重清前同样检查前台
- 优化了深度清理的候选门槛，与日常模式分层

### 效率与学习

> 稳态锚点与回弹学习让无效清理接近归零。程序学习每个应用的自然内存占用基线，低于基线的自动跳过，回填型应用自动暂停重复清理，应用更新后基线自动迁移。

- 新增了稳态抑制学习，常驻回填程序自动跳过无效清理
- 新增了回弹自动回退，清完总是迅速回填的应用自动暂停重复清理，恢复后自动解除
- 优化了学习适应性，应用更新后稳态基线自动迁移，不会误判
- 优化了学习验证，暂停与抑制期间周期性验证，行为变化快速恢复

### 清理模式与系统

> 四种模式真实力度分层。极限模式不再等待立即全力清理、深度模式等待减半、日常保持稳妥；高频阶段只做轻量操作，系统级全清受压力与手动双重门控。

- 优化了四种清理模式的力度分层，极限立即清理、深度减半、日常稳妥
- 优化了极限模式的清理深度，每进程按最大轮数深度清理
- 优化了极限模式的价值底线，保留最低价值门槛，防完全无意义清理
- 优化了手动优化策略，主动清理时跳过等待类保护立即全力执行
- 优化了高频阶段的操作范围，文件缓存与卷冲刷仅在周期收割执行
- 优化了系统级全清的门控，自动仅压力中等以上执行，手动不受限
- 优化了手动优化的知情权，执行前显示将清理的候选范围
- 新增了紧急触发兜底，可用内存过低同样触发紧急清理（可配置）

### 自动调参与界面

> 自动调参增加抑制自平衡闭环，界面细节与术语规范全面修正。

- 优化了自动调参兜底，抑制过多导致内存压不下去时自动放开
- 修复了自动调参的协方差误判，语义同向的调整不再被错误冻结
- 修复了进程排行与学习日志的列对齐，语言切换后列配置保持正确
- 优化了界面提示，多处描述与实际行为一致，术语呈现规范统一
- 修复了停止按钮图标在英文界面显示异常的问题

### 兼容与稳定

> 单实例跨版本兼容与系统级清理边界修正。

- 优化了单实例的跨版本兼容，旧版运行时新版启动会激活旧窗口并退出
- 修复了系统级全清在内存充裕时可能误清正在使用程序的问题

---

### 构建

- 版本号 v3.9.28 → v4.0.128（同步面：窗口标题/命令行/文档标题/启动日志）
- 134 项回归测试全绿，零编译错误
- 发布版 exe = 冷启动默认配置（不打包本机配置）
