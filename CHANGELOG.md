# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.2.6 (2026年8月)

> v3.1.5 发布后的维护增量：游戏进程管理补齐一体化名单窗口（此前仅全屏检测 + 手动开关，名单需手改配置）；图标体系全面重构——M 字样下移校准至参考图位置、小尺寸扁平化消除明暗斑驳、任务栏图标独立优化（48px 立体 + 提亮 + 锐化）。共 2 大项 6 项维护，回归测试 37 项全绿。

### 游戏进程管理：一体化名单窗口

> 游戏模式此前仅支持全屏自动检测与手动开关，自定义名单需手改配置文件。本轮新增一体化管理窗口，名单操作图形化完成。

- **一体化窗口**：设置 → 游戏模式 → 「管理游戏进程」打开管理窗口，自定义条目可视化列出
- **增删便捷**：支持批量添加（按分隔符拆分）与逐条删除（确认弹窗），实时写入配置并生效
- **内置名单只读**：程序自带名单只读展示，不可误改；自定义与内置叠加生效

---

### 图标体系重构：母版重绘 + 自适应扁平化

> 图标全面重构。M 字样下移校准至参考图位置，圆盘保持 hq6 光照风格；小尺寸启用扁平渲染，消除明暗斑驳；任务栏图标独立优化，深色任务栏上清晰锐利。

- **母版重绘**：M 骨架 `off_mult` 2.01→0.81（中心偏上 -32.2px → -13px），全尺寸同构；`icon_master_256/128.png` 与 `assets/icon.ico` 7 帧由 `scripts/_icon_master.py` + `_icon_build_ico.py` 参数化生成
- **小尺寸扁平**：≤48px 无投影、纯色圆盘、近白 M（管状光照仅大尺寸保留）；`m_scale` 0.87 等比收窄防横向显扁，GUI 窗口/对话框/托盘图标自动应用
- **任务栏优化**：窗口大图标 48px 立体渲染（150% DPI 1:1 显示）+ 圆盘提亮（62→88）+ unsharp 锐化（仅任务栏路径，经 `force_large`/`sharpen` 参数）
- **ico 帧分配**：16 帧扁平（小图标），24/32/48 帧立体直接渲染（任务栏小/标准/高 DPI，非缩图防糊），64/128/256 立体（桌面/大图标）

---

### 构建

- 版本号 v3.1.5 → v3.2.6（Mutex/窗口标题/CLI/README/测试同步，新 mutex 避免与旧版互斥冲突）
- 发布版 exe = 冷启动默认配置（`datas=[]` 不打包本机 config.yaml，首次运行回退 `DEFAULT_CFG`）
- PyInstaller 6.21.0，Python 3.14.0
- 37 项回归测试全绿，零编译错误
