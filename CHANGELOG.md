# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.0.1 (2026年8月)

> 本轮是 v3.0 发布后的两轮全量深度审查修复：第一轮聚焦决策链与 EFIS 的潜伏失效（上下文修正取错桶、权重学习零影响、场景系统死代码、参数持久化脱节），第二轮聚焦逻辑层顽固缺陷（游戏模式缓存泄漏、崩溃恢复误续守护、周期数据滞后）。累积修复 20 项，回归测试保持 37 项全绿，版本号升级 3.0.1 并重建 exe。

---

### 决策链修复：上下文修正与投票学习复活

`thompson_score` 的上下文修正此前读取从未被填充的 `learner._ctx`（恒取 50% 内存、非前台桶），且策略投票的在线权重学习更新后从不参与决策——两处机制形同虚设。

- **`thompson_score` 参数化**：`mem_pct/is_fg` 由调用方传入真实上下文，修正因子取实际桶；`judger.update_pressure` 同步 `_ctx` 兜底，6 个消费点全覆盖
- **策略投票加权决策**：`should_trim` 改用在线学习的权重加权求和；权重直接按"预测贡献 × 实际结果"调节（有界 ±2、可为负），学习真实影响投票
- **内存趋势恢复**：`update_pressure` 维护 30s 采样 EWMA 趋势，树2 趋势分支获得真实数据
- **时间遗忘语义修正**：距上次反馈（清理/试探）超 1 小时才向先验回归，原误用 `last_seen` 导致活跃进程永不遗忘

### EFIS 完整化：场景激活、边界收敛、持久化闭环

- **场景系统激活**：`detect_scene` 接入守护周期（原无调用点，scene_params 全失效），游戏/浏览器/开发/常规场景 7:3 混参独立调参
- **症状顶格预检**：参数已到边界时症状直接清除（原无界累积，实测 `pid_kd+` 涨到 264 并持久化）
- **参数持久化闭环**：启动时用 `efis_state.json` 对齐 `judger.cfg` 与 `learner._kalman_r`；调参后同步写盘 `config.yaml`——重启不再回默认值
- **`kalman_r` 生效**：EFIS 调出的观测噪声在新画像上持久生效（原只在调参轮短暂生效）

### 游戏模式完善：手动持久、Layer3 静音、优先级恢复

- **手动模式持久生效**：手动开关后自动检测不再覆盖（原 2 轮内被自动关闭/重开），游戏退出时清空失效 PID 保护集防复用误保护，PID 集仍实时刷新
- **Layer3 静音化**：`_layer3_deep` 预处理补 `game_mode` 保护——`deep_compress`（flush_modified+purge_standby）与脏页写回在游戏模式全部跳过，"系统缓存零触碰"覆盖全链路
- **游戏 PID 优先级恢复**：此前被降级（游戏启动前被当普通进程）的游戏进程恢复默认内存优先级与 EcoQoS，不再被 OS 优先回收

### 守护与生命周期

- **harvest 后刷新内存状态**：周期日志/图表/EFIS 诊断输入用最新值（原用 gap 末旧值，最长滞后 30s）
- **崩溃恢复按状态续守护**：`watchdog.json` 记录崩溃前守护状态，看门狗重启链路保留该字段——恢复实例只在守护运行中才自动续守护，尊重用户意图
- **双向防重入**：手动优化进行中禁止启动守护（原可并发 optimize，共享线程池同 PID 双 trim）
- **内存通知句柄释放**：守护线程退出时关闭 `h_low/h_high`（原每次启停泄漏 2 个内核句柄）

### 界面与防御

- **进程排行双击终止**：单击仅选中，双击才询问终止（原单击即弹确认，有误杀风险）
- **弹窗图标复用**：`_apply_icon` 进程级缓存 HICON（原每次弹窗创建 2 个 GDI 对象且不销毁）
- **配置并发写锁**：`config.save` 加线程锁（EFIS 调参线程与 GUI 主线程不再争用同一 tmp 文件）
- **统计真实化**：`ws_trim` 按 `empty_all_working_sets` 实际成功计数（原权限不足时虚增）
- **PF 无基线兜底**：probe/trim 的 PF 基线读取失败时跳过副作用判定（原误进失败冷却）

### 统计口径：系统杂项囊括全部系统操作

系统杂项此前只统计 `standby`/`modified`/`combine` 等部分操作——`volume`（卷缓存）、`registry`（注册表）、`compress`（压缩）、`filecache`（文件缓存）实际执行却不计数，统计栏数字与设置面板 8 个勾选项脱节。

- **计数补齐**：`_layer1_memreduct` 的 `volume`/`registry`/`compress`/`filecache` 分支按 API 真实成功计数（原只执行不写统计）
- **口径对齐**：系统杂项 = 设置面板 8 勾选项中除 `ws`（归"进程"栏）外的 7 类系统操作的真实成功次数之和，勾选即执行、执行成功即计入

---

### 构建

- 版本号 v3.0 → v3.0.1（Mutex/窗口标题/CLI/README/测试同步，新 mutex 避免与旧版互斥冲突）
- PyInstaller 6.21.0，Python 3.14.0，产物约 12.7 MB（`dist/MemWise.exe`）
- 37 项回归测试全绿，全项目零编译错误
