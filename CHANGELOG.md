# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.3.10 (2026年8月)

> v3.2.6 发布后的维护增量：修复主窗口与全部子窗口"先显示后居中"的闪烁；日志系统升级为统一运行日志（「记录运行日志到文件」开关总控，2MB×2 轮转）。共 2 大项 10 项维护，回归测试 37 项全绿。

### 窗口闪烁修复

> 主窗口与全部子窗口（设置/排除列表/游戏进程管理等）此前"先在系统默认位置显示，再跳到居中位置"，且控件逐个挂载时窗口边生长边可见，形成双重闪烁；托盘驻留较久后恢复还可能被系统重置位置。

- 修复了主窗口与全部子窗口"先显示默认位置再居中"的闪烁（创建即隐藏 → 居中定位 → 一次显示）
- 修复了托盘恢复时窗口位置错乱（恢复路径先隐藏 → 重新居中 → 显示）
- 修复了 `run()` 访问未初始化属性的崩溃（`_minimized_to_tray` 显式初始化）

---

### 统一运行日志

> 清理日志此前仅记每轮释放量且无限追加；崩溃诊断与日常日志分处两文件，时间线割裂、排查要来回翻。本轮合并为统一运行日志，由「记录运行日志到文件」开关总控。

- 新增了统一运行日志（`memwise.log`，程序目录），由设置「记录运行日志到文件」开关总控：开启后全量记录（启动/退出、每轮清理摘要、界面消息、游戏模式决策、EFIS 调参、配置保存、异常与崩溃现场、诊断），关闭则不写任何日志；毫秒时间戳 + 类别标签（`[启动][清理][决策][调参][配置][异常][系统][诊断][界面]`）
- 新增了容量轮转（2MB × 2 份，`memwise.log.1` 保留最近两份自动删除最旧，零数据丢失）
- 新增了线程安全写入（`threading.Lock` + 行缓冲，多线程不交错损坏；写入时 `fstat` 检查轮转，零额外线程）
- 新增了首启迁移（旧 `memwise_crash.log` 自动并入统一日志，历史现场不丢失）
- 优化了崩溃诊断（faulthandler/未捕获异常并入统一日志，不再依赖 `memwise_crash.log`）
- 移除了旧 `memwise.log` 简易周期日志（每轮仅记 freed MB，被完整周期摘要取代）；「记录运行日志到文件」开关升级为统一日志总开关，运行时切换即时生效
- 修复了无崩溃时 `memwise_crash.log` 也写入启动信息（并入统一日志后名实相符）

---

### 构建

- 版本号 v3.2.6 → v3.3.10（Mutex/窗口标题/CLI/README/测试同步，新 mutex 避免与旧版互斥冲突）
- 发布版 exe = 冷启动默认配置（`datas=[]` 不打包本机 config.yaml，首次运行回退 `DEFAULT_CFG`）
- PyInstaller 6.21.0，Python 3.14.0
- 37 项回归测试全绿，零编译错误
