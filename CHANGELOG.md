# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

---

## v2.5 (2026年7月)

> 本轮的焦点是工程健壮性与极致化打磨——建立三层超时防御体系消除守护周期中任何可能的卡死风险，打通超时→正向反馈的完整闭环，修复多项长期潜伏的逻辑不一致与边界崩溃隐患，并在管线细节中挤出最后一点优化潜力。不做架构大改，但每一处改动都让引擎运转得更安全、更紧凑、更可预期。

---

### 时序超时防御体系

守护周期面临的最坏情况——某个进程在内核态 `EmptyWorkingSet` 卡死不返回——此前没有任何保护。本轮建立了覆盖全链路的超时防线：

- **单进程 trim 8 秒 / probe 4 秒硬超时**：`future.result(timeout=8/4)`，超时后跳过该进程、标记失败、Thompson 惩罚，不阻塞后续进程队列
- **harvest optimize 30 秒硬超时**：每轮最后一次全量收割用独立线程执行 + 30 秒超时包装，超时后返回 `_partial` 标记，gap-fill 阶段累积的成果仍正常计入
- **周期补偿机制**：`overtime_debt` 追踪累计超时秒数（上限 30 秒），下一轮 deadline 自动扣除。提前完成则债务递减，两轮内回归 60 秒节奏
- **超时后跳过 EFIS/MetaCognition 诊断**：harvest 超时产生的零数据不污染自适应调参，效率值正常显示（零值真实反映超时影响）

---

### 超时正向反馈闭环

超时不只防御，更让 Learner 从中学习：

- **递增冷却**：`timeout_count`（仅内存不持久化）记录超时次数，`mark_failed(fail_count=tc)` 冷却倍数从 1× 递增至 4×。成功清理后 `mark_trimmed` 自动衰减计数
- **Thompson 惩罚**：超时进程执行 `record_clean_result(ok=False)`，beta+1 降低 θ，Kalman 过程噪声 q 提升增加不确定性估计
- 同时保留 `can_trim` 判定——超时进程只是冷却更长、θ 更低，不会被永久拉黑，仍有机会在后续轮次重新尝试

---

### 效率折线图逻辑修复

此前折线和折点使用了不一致的索引体系：折点正确使用全局绝对索引判断前 3 轮虚线，而折线错误地使用局部可见索引（`min(n_pts, 3)-1`）。这导致图表随数据积累左移后，可见区域最左侧的折线永远被误画为虚线。

修复为统一使用 `eff_start_idx + i` 全局索引——线段端点全局索引 ≤ 2 的才画虚线，其余实线。前 3 轮收敛期虚线样式现在只在真正的会话起始轮次出现。

---

### 并发引擎优化

- `_max_workers` 从死代码复活：此前定义为 `min(cpu_count, 4)` 但两个 ThreadPoolExecutor 各写死 `max_workers=4`，变量从未被使用。现统一引用，上限提升至 `min(cpu_count, 8)`
- 多核机器（6-8 核）上进程 probe/trim 扫描吞吐量显著提升

---

### 核心管线细化

- **管线末尾自清理**：`_layer1_memreduct` 末尾追加 `EmptyWorkingSet(os.getpid())`，释放 MemWise 自身占用的几十 MB 工作集
- **WS 回弹阈值对齐**：`_layer3_deep` 的 WS 回弹触发阈值从 1.5× 修正为 2.0×，与 `can_trim` 中已有的 `ws_override` 阈值一致，减少 Layer3 误触发（WS 自然波动 20-30% 不应触发深层清理）
- **PF 成本模型浮点修复**：probe 和 trim 的 PF 公式中将整数除法 `freed // (1<<20) * 10` 改为浮点 `int(freed / (1<<20) * 10)`，小释放（<104MB）也能按比例判断 PF 是否超标
- **WS 基线过期缩短**：30 分钟 → 20 分钟，快速回填进程（浏览器等）更快被重新纳入清理候选

---

### 守护参数优化

- **gap 间隔**：正常模式 15→12 秒，游戏模式 22→18 秒（自动 1.5×），每轮 gap-fill 阶段多做 1 次 `optimize(normal)`
- **deadline 修正**：此前 deadline 使用 CFG 的 `interval=30` 计算（仅 27 秒），而非实际 60 秒周期。现统一为 60 秒，gap-fill 窗口从 ~21 秒扩展到 ~51 秒
- **gap 可配置化**：设置面板新增"守护清理间隔"滑条（8-20 秒，默认 12），游戏模式自动 ×1.5。两处设置面板（设置+排除列表）均已加入

---

### 清理深度天花板对齐

此前 `clean_passes` 滑条设到 5-6 无实际效果——算法内部写死超大进程最多 4 pass。现超大进程（WS>200MB 或 θ>deepen_theta）的 pass 数直接等于用户设定值，`total_wait` 等比缩放控制 PF。中进程和低价值进程保持不变（3/2/1 pass）。滑条 5-6 现在有实际意义。

---

### 泄漏检测消息修复

`Profile.feed()` 中检测到泄漏时写入的 `_info_msgs` 从未被收集——`PareLearner.pop_info()` 只取了 Learner 自身的消息列表。修复为遍历 `self.profiles.values()` 收集并清空各 Profile 的消息缓冲，泄漏检测提示不再静默丢失。

---

### 构建

- 版本号 v2.4 → v2.5
- MemWise.spec `hiddenimports` 清理残留的 `win32api`（项目用 ctypes 不需要 pywin32）
- PyInstaller 6.21.0，Python 3.14.0
- 产物约 12.7 MB
