# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.7.09 (2026年8月)

> v3.6.47 发布后的维护增量：以「新用户单 exe 顺畅使用」为目标完成全量审查，修复了开机自启失效、每轮清理量下降、进程排行可终止系统进程等 9 项问题；界面快捷键与周期提示全面与实际设置同步。回归测试 69 项全绿。

### 清理与周期

> 实测发现守护周期设置在此版本起真正生效，旧配置的 30 秒周期使每轮清理量按比例减半；同时清理任务在进程较多时会被提前截断，两项叠加造成每轮清理数量与释放量明显下降。

- 修复了每轮清理数量与释放量下降的问题——清理任务不再因超时被提前截断，进程多时也会完整处理
- 优化了守护周期配置——按你的确认恢复默认 60 秒完整周期，每轮清理量回到正常水平（原配置已备份）

### 开机自启与安全

> 普通开机自启（非管理员）自很早的版本起一直无法生效——设置后启动文件夹里不会出现快捷方式，因管理员自启一直在用所以从未暴露；进程排行窗口双击进程可终止系统进程，存在误杀风险。

- 修复了普通开机自启设置后无法生效的问题——现在创建的开机快捷方式指向、参数、工作目录均验证正确
- 修复了进程排行中可终止系统进程的安全问题——系统核心进程（含资源管理器等）现在禁止被终止

### 清理算法与状态

> 两处细节修正：对快速增长进程的"快速增长"判定条件几乎恒真——任何微小的内存增长都会被当作快速增长而获得清理优先级加成，导致排序失真；手动优化时，后台实际执行的清理模式与界面下拉框显示的模式可能不一致（界面状态由多个线程读取，存在不同步风险）。

- 优化了快速增长进程的识别逻辑——只有相对增长达到一定比例才会被优先清理
- 修复了手动优化时界面模式与实际执行不一致的问题

### 代码卫生与隐私防护

> 两处防事故处理：设置面板的清理操作项中存在一个从未生效的无效项；另一次维护中，用户配置文件备份曾被误带入公开代码仓库——配置属于个人数据（含自定义游戏名单等），公开仓库混入会造成隐私泄露。

- 移除了一个无效的内部清理操作项
- 修复了用户配置文件可能被误上传的隐私问题——配置与备份文件已整体排除在代码仓库之外

### 界面提示

> 全局快捷键与守护周期都可在设置中更改，但界面多处提示仍写死为默认值——修改后提示与实际不符。

- 修复了界面快捷键与守护周期提示与实际设置不一致的问题——状态栏与提示文字不再写死默认值，随设置实时同步

---

### 构建

- 版本号 v3.6.47 → v3.7.09（同步面：互斥锁/窗口标题/命令行/文档标题）
- 69 项回归测试全绿，零编译错误
- 发布版 exe = 冷启动默认配置（不打包本机配置）
