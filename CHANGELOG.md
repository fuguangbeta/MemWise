# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.8.40 (2026年8月)

> v3.7.09 发布后的维护增量：对全项目完成两轮全量审查，并聚焦核心清理链路做算法级迭代、重做游戏模式，共 40 项——深度清理边界、并发稳定性、节能标记从未生效等隐蔽问题全部修复；进程快照采集、紧急响应、清理调度全面提速；游戏模式从"一刀切停工"重做为"进程树绝对保护 + 非游戏进程持续腾挪"。回归测试 71 项全绿。

### 深度清理与边界保护

> 深度清理阶段此前会绕过两道用户设定：关闭「释放进程闲置内存」后深度清理仍会触碰进程；加入排除列表的进程在深度清理中仍可能被清理。另有一处会误清理本程序自身。

- 修复了关闭「释放进程闲置内存」后深度清理仍会清理进程的问题——开关在所有清理阶段一致生效
- 修复了被排除的进程在深度清理中仍可能被清理的问题——排除列表在所有清理阶段一致生效
- 修复了深度清理可能误清理本程序自身的问题

### 并发与稳定性

> 守护运行期间打开学习日志或进程排行窗口时，界面与后台守护线程并发读写共享数据，存在偶发无响应、CPU 占用显示失真、统计数字少计等问题；托盘图标与关机流程另有隐患。

- 修复了守护运行中打开学习日志可能偶发无响应的问题
- 修复了进程排行的 CPU 占用显示可能失真的问题
- 修复了统计数字偶发少计的问题（守护中手动清理与后台清理同时发生时）
- 修复了切换窗口后内存优先级恢复可能滞后的问题
- 修复了托盘图标偶发失效的隐患（长期运行句柄累积）
- 修复了关机取消后崩溃自动恢复可能失效的问题
- 修复了进程号复用导致的 CPU 占用显示失真问题

### 决策与配置

> 游戏模式参数中多处配置从未真正生效或只写不读；程序启动后自动调参结果可能被旧配置覆盖；失败进程的冷却策略判定过松；命令行守护参数说明与实际不一致。

- 移除了一个从未生效的游戏模式配置项
- 移除了一个与默认行为重复的游戏模式配置项
- 移除了一个无实际作用的游戏进程状态记录
- 修复了自动调参结果在守护运行中偶发回退的问题
- 优化了失败进程冷却策略——只有真正快速回填内存的进程才会缩短冷却
- 修复了命令行守护参数说明与实际不一致的问题
- 优化了内部监控逻辑——移除了无效的输入参数

### 识别与效率

> 内置游戏名单包含 Java 运行时的通用进程名，任何 Java 程序运行都会误触发游戏模式；卷缓存刷新会尝试网络盘与光驱。

- 修复了任意 Java 程序都会误触发游戏模式的问题——Java 版 Minecraft 可在自定义名单中添加识别
- 优化了卷缓存刷新——跳过网络盘与光驱，避免偶发卡顿

### 界面与文档

> 守护运行中「已学习」计数不随周期刷新；说明文档中清理模式表、学习日志、守护间隔等描述与实际行为不一致；恢复出厂设置会覆盖旧备份。

- 修复了守护运行中「已学习」进程数不更新的问题
- 修复了说明文档中清理模式表描述与实际行为不一致的问题
- 修复了说明文档中学习日志描述与实际显示不一致的问题
- 修复了说明文档中守护间隔区间描述不一致的问题
- 优化了恢复出厂设置的备份策略——多次重置备份互不覆盖

### 核心优化与内存治理

> 全链路审查发现核心清理链路的几处瓶颈：进程快照逐进程读取导致每轮开销大；内存飙高时紧急清理要等下一周期才响应；文件缓存释放量统计受其他进程干扰；进程排序无视缺页代价、多轮清理清"热页"低效、并发与重评间隔固定。

- 优化了进程快照采集——内存与 CPU 数据一次批量获取，每轮开销大幅降低
- 优化了紧急清理响应——内存使用率飙到阈值时立即执行全量清理，不再等待下一轮
- 优化了文件缓存清理的释放量统计——直接测量系统缓存缩减，数字更真实
- 修复了深度清理中文件缓存回收偶发失效的问题——回收后正确恢复系统默认缓存上限
- 优化了进程清理排序——同等释放量下优先清理缺页代价更低的进程
- 优化了多轮清理策略——回填不足的进程不再重复清理热页，避免无效开销
- 优化了清理并发度——按内存压力自适应调整，减少系统卡顿
- 优化了重复清理判定——按进程回填速度自适应调整重评间隔，慢回填进程不再被频繁打扰

### 节能与内存策略

> 按学习评分设置的节能标记此前因系统接口差异从未真正生效（隐蔽失效）；修复生效后全量启用又会使系统把大量冷页压缩驻留（压缩内存计入"正在使用"），推高内存使用率——经实测将应用范围收敛到高价值大进程。

- 修复了节能标记从未真正生效的问题——按学习评分对进程启用节能回收提示
- 优化了节能标记的应用范围——只对高价值大进程启用，避免压缩驻留推高内存使用率
- 优化了清理并发策略——低压力时保持足够吞吐，持续压缩能力不下降
- 优化了多轮清理的判定阈值——慢回填进程更少被误判为低效清理

### 游戏模式

> 游戏模式此前对清理"一刀切停工"——进入游戏后非游戏进程也几乎不再被清理（每周期仅一次收割），优化量趋近于零。实际游戏流畅只依赖两件事：游戏进程树不被触碰、磁盘干扰类系统操作被跳过；非游戏进程的清理根本不产生游戏卡顿（缺页发生在被清进程自己身上）。

- 修复了游戏模式下非游戏进程几乎不被清理的问题——非游戏进程恢复持续积极清理，为游戏腾出内存
- 优化了游戏保护范围——游戏进程及其全部子进程树（反作弊、更新器等）一并绝对保护
- 修复了手动模式下游戏新启动子进程可能未被保护的问题——手动与自动模式保护集实时刷新一致
- 修复了游戏进程识别对无后缀条目不生效的问题——手改配置或旧数据的进程名也能精准匹配

---

### 构建

- 版本号 v3.7.09 → v3.8.40（同步面：互斥锁/窗口标题/命令行/文档标题）
- 71 项回归测试全绿，零编译错误
- 发布版 exe = 冷启动默认配置（不打包本机配置）
