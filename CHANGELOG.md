# MemWise 更新日志

> **MemWise** — Windows 智能内存看护工具。不杀进程、不挂起线程、不注入、不联网。
> 纯 ctypes Win32 API，零外部依赖。

## v3.7.09 (2026年8月)

> v3.6.47 发布后的维护增量：以「新用户单 exe 顺畅使用」为目标完成第四、五轮全量审查，实测发现并修复普通自启自 v3.3.10 以来的静默失效（COM GUID 字节错误）、滑动窗口分批提交截断清理量、进程排行可终止系统进程等 9 项问题；界面热键/周期文案全面配置驱动化。回归测试 69 项全绿。

### 清理量与统计

> 实测发现守护周期配置 `interval` 自 v3.6.47 起真正生效（旧版被硬编码覆盖），用户旧配置 30 秒导致每轮工作量减半；同期滑动窗口分批提交的 20 秒总预算硬限会截断候选进程、超时项不计入统计——两项叠加造成每轮清理数与释放量明显下降。

- 修复了滑动窗口分批提交的总预算截断清理量——`_bounded_submit` 原 `total_budget=20s` 硬限改为 `idle_timeout=60s` 无进展超时（仅当窗口内既无任务完成也无任务开始时判定池卡死退出），正常任务全量处理零截断，防卡死能力保留（probe/trim/Layer3 三处调用同步）
- 调整了守护周期配置——用户旧配置 `interval: 30` 现按配置生效，每轮时长减半导致清理量按比例下降；经确认改回 60（改前已备份），每轮完整周期恢复

### 自启与安全

> 实测 `set_auto_start`（普通开机自启）自 v3.3.10 起一直静默失效：两个 COM 接口 GUID 字节错误——`_IID_IShellLinkW` 误用 `{EE80CA92-...}`（应为 `{000214F9-...}`）致 `CoCreateInstance` 返回 `E_NOINTERFACE`，`_IID_IPersistFile` 字节序写反致 `QueryInterface` 失败；因用户一直使用管理员计划任务自启，缺陷从未暴露。进程排行双击终止仅保护 PID≤4 与自身，系统进程可被确认强杀。

- 修复了普通（非管理员）开机自启静默失效——两个 IID GUID 改正（标准 `{000214F9-0000-0000-C000-000000000046}` 与 `{0000010B-...}` 小端字节），实测创建快捷方式并读回 `Target/Args/WorkingDirectory` 全部正确（vtbl 索引实测无误保留）
- 修复了进程排行双击终止可终止系统进程——接入 `_is_system_core()` 统一保护名单（svchost/explorer/dwm 等禁止终止，防误杀系统进程）

### 算法与线程

> 预判式清理的「快速增长」加分阈值用字节绝对值（0.002 字节/tick 几乎恒真）名不副实；手动优化线程与守护线程同款读取 Tk 变量（非线程安全，此前仅守护线程修复）。

- 修复了预判式清理增长加分的阈值语义——`slope > 0.002`（字节/tick 绝对值，几乎恒真）改为相对增长率 `slope > s.ws * 0.002`（0.2%/tick），与「快速增长」注释语义一致
- 修复了手动优化线程读取 Tk 变量（`_opt_worker` 中 `mode_var.get()` 非线程安全，与守护线程同规则）——改读 `CFG["clean_mode"]` 配置镜像

### 代码卫生与防护

> 两个防事故项：`standby_low` 是非六开关的死键；`git add -A` 曾把用户配置备份带入公开仓库（隐私红线）。

- 移除了 `standby_low` 死键（非 6 清理开关之一，永不可能出现在操作集合）
- 补强了 `.gitignore`（`config/*.bak*` 与全局 `*.json`）——用户配置/备份/状态文件整体忽略，公开仓库永不混入用户隐私数据

### 界面文案

> 热键与守护周期均可配置，但 6 处界面文案硬编码「Ctrl+Shift+M」/「每 60 秒」——用户改配置后界面与实际行为不符（名不副实）。

- 修复了热键/守护周期文案名不副实——新增 `_hk_display()`/`_interval_display()` 配置驱动辅助，状态栏「就绪」文案×3、游戏模式热键 tooltip、守护/自启守护 tooltip「每 60 秒」全部动态化

---

### 构建

- 版本号 v3.6.47 → v3.7.09（同步面：Mutex/窗口标题/FindWindowW/FindWindowExW/CLI/docstring/测试 docstring/README 标题，新 mutex 避免与旧版互斥冲突）
- 69 项回归测试全绿，零编译错误；`set_auto_start` 实测通过；冷启动 8 项模拟通过
- 发布版 exe = 冷启动默认配置（`datas=[]`，不打包本机 config.yaml）
